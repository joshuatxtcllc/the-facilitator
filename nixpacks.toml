[phases.setup]
nixPkgs = ["python311", "pip"]

[phases.install]
cmds = ["cd prompt-orchestrator && pip install -r requirements.txt"]

[start]
cmd = "cd prompt-orchestrator && uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8080}"
