[build]
builder = "NIXPACKS"

[deploy]
restartPolicy = "ON_FAILURE"

[env]
PYTHONUNBUFFERED = "1"
PORT = "8080"

[[services]]
name = "orchestrator"
port = 8080
[services.deploy]
command = "uvicorn app.main:app --host 0.0.0.0 --port 8080"
